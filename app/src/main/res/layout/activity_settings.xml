<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_marginTop="24dp"
        android:layout_width="match_parent"
        android:layout_height="@dimen/subtitle"/>
    <LinearLayout
        android:layout_below="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingTop="@dimen/list.padding.vertical"
        android:paddingBottom="@dimen/list.padding.vertical"
        android:orientation="vertical">
        <TextView
            android:layout_width="match_parent"
            android:layout_height="@dimen/subtitle_item"
            android:textSize="@dimen/text"
            android:textColor="@color/content_section"
            android:text="@string/settings.section.general"
            android:gravity="center|left"
            android:fontFamily="sans-serif-medium"
            android:layout_marginTop="@dimen/area_space"
            android:paddingLeft="@dimen/list.item.padding.horizontal"
            android:paddingRight="@dimen/list.item.padding.horizontal"/>
        <LinearLayout
            android:id="@+id/settings.fetch.content.time"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingLeft="@dimen/list.item.padding.horizontal"
            android:paddingRight="@dimen/list.item.padding.horizontal"
            android:paddingTop="@dimen/list.item.padding.vertical"
            android:paddingBottom="@dimen/list.item.padding.vertical"
            android:orientation="vertical">
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/settings.time.title"
                android:textSize="@dimen/list.item.primary.text"
                android:textColor="@color/black"/>
            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="0 minutos"
                android:textSize="@dimen/list.item.secondary.text"
                android:textColor="@color/content"/>
        </LinearLayout>
    </LinearLayout>

</RelativeLayout>