<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
      tools:context=".MainActivity">
    <item
        android:id="@+id/menu.item.refresh"
        android:title="@string/menu.action.refresh"
        android:icon="@drawable/ic_autorenew_black_24dp"
        android:orderInCategory="0"
        app:showAsAction="always"/>
    <item
        android:id="@+id/menu.item.scheduled"
        android:title="@string/menu.action.scheduled"
        android:orderInCategory="100"
        app:showAsAction="never" />
    <item
        android:id="@+id/menu.item.settings"
        android:title="@string/menu.action.settings"
        android:orderInCategory="200"
        app:showAsAction="never" />
</menu>
